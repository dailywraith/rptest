on:
  push:
    branches:
      - develop

name: release-please

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: google-github-actions/release-please-action@v4
        id: release
        with:
          token: ${{ secrets.PAT }}

      - uses: actions/checkout@v4
        with:
          ref: ${{ fromJSON(steps.release.outputs.pr).headBranchName }}

      - run: echo "version=`jq -r '.version' package.json | cut -d '.' -f 1,2`" >> $GITHUB_OUTPUT
        id: version

      - run: echo `jq '.build.qa.channel="qa-v${{ steps.version.outputs.version }}.0" | .build.production.channel="production-v${{ steps.version.outputs.version }}.0"' eas.json` > eas.json

      - run: cat eas.json

      - run: echo "kick off eas builds for qa"
        if: ${{ steps.release.outputs.prs_created }}

      - run: echo "kick off uat build once release is on develop branch ${{ fromJSON(steps.release.outputs).releases_created }}"
        if: ${{ steps.release.outputs.releases_created }}
